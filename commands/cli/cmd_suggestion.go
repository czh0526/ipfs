package cli

import (
	"fmt"

	cmds "github.com/czh0526/ipfs/commands"
)

func printSuggestions(inputs []string, root *cmds.Command) (err error) {
	fmt.Errorf("printSuggestions 未实现... inputs = %v", inputs)
	return
}
